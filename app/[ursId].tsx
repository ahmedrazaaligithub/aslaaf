// import { useSearchParams } from 'expo-router';
import { View, Text } from 'react-native';
import React from 'react';

const UrsIds = () => {
  // const { ursId } = useSearchParams(); // Retrieve the dynamic id

  return (
    <View>
      <Text>Urs ID:</Text>
    </View>
  );
}

export default UrsIds;
